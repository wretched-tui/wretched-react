{
  "name": "demos",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist/",
    "build": "yarn clean && yarn tsc",
    "demo": "yarn build && cd dist/ && node --enable-source-maps -- index"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-reconciler": "^0.29.0",
    "wretched": "^1.0.9-alpha",
    "wretched-react": "file:../dist"
  },
  "devDependencies": {
    "typescript": "^5.4.3"
  }
}

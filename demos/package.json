{
  "name": "demos",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && yarn tsc",
    "checksum": ". ../scripts/check.sh .",
    "demo": "yarn checksum && cd dist && node --enable-source-maps -- index",
    "clean": "rm -rf dist/"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-reconciler": "^0.29.0",
    "wretched": "^1.0.0",
    "wretched-react": "file:../dist"
  },
  "devDependencies": {
    "typescript": "^5.4.3"
  }
}
